# Ralph Progress Log
Started: Tue Mar 10 12:11:06 AM CET 2026
---

## Codebase Patterns
- Integration tests asserting on human output text must use `.env("NO_COLOR", "1")` + `--no-color` to strip ANSI codes
- gen-deploy success output goes to stderr via `eprintln!` and `output::print_stage` (not stdout)
- `TemplateContext` struct has `is_library: bool` and `proxy: Option<String>` fields available at the output point
- Instruction lines after gen-deploy success are plain text to stderr (no styling), matching Go behavior
- `\u{2705}` is ✅ (CHECK emoji), `\u{1f4dd}` is 📝 (MEMO), `\u{1f528}` is 🔨 — used as raw Unicode escapes in `print_stage` calls
- migrate config success uses `styled()` + `color::SUCCESS` (green bold) with `emoji::CHECK_MARK` (✓), NOT `print_stage` with ✅
- migrate config human output goes to stdout via `println!`, not stderr (unlike gen-deploy which uses stderr)
- migrate config warnings go to stderr via `eprintln!`, matching Go's `fmt.Fprintln(os.Stderr, ...)`
- `write_or_print_v2_with_prompt` has two sequential prompts when treb.toml exists: overwrite prompt, then write/preview prompt

## 2026-03-10 - P12-US-001
- Ported gen-deploy human output to match Go format
- Changed "Generated deploy script" → "Generated deployment script"
- Added leading newline before success emoji
- Added type-specific instruction lines (library: CREATE2 message, proxy: implementation+proxy message + initializer note)
- Added "To deploy, run:" command line for all types
- Files changed: `crates/treb-cli/src/commands/gen_deploy.rs`
- **Learnings for future iterations:**
  - The gen-deploy output point is around line 874-902 in gen_deploy.rs, inside the `else` branch of the `if json` check
  - `context.is_library` and `context.proxy` are available at the output point from the `TemplateContext` struct
  - `print_stage` uses `eprintln!` (stderr), so all new instruction lines should also use `eprintln!` for consistency
---

## 2026-03-10 - P12-US-002
- Ported migrate config success, cleanup, and next-steps output to match Go format
- Changed success message from `print_stage("✅", "Migration complete...")` to green bold `✓ treb.toml written successfully` via `styled()` + `color::SUCCESS`
- Added green bold `✓ foundry.toml cleaned up` message when foundry cleanup is performed
- Added "Next steps:" section with conditional numbering (2 items if cleaned up, 3 if not)
- Added "Generated treb.toml:" header before interactive preview
- Changed cancellation text from "Cancelled." to "Migration cancelled."
- Added `styled()` helper, `owo_colors::OwoColorize`, `color`, and `emoji` imports to migrate.rs
- Files changed: `crates/treb-cli/src/commands/migrate.rs`
- **Learnings for future iterations:**
  - migrate.rs success output section starts around line 287 in `write_or_print_v2_with_prompt`
  - `foundry_cleanup_path.is_some()` determines whether foundry was cleaned up (controls next-steps numbering)
  - JSON output is unchanged — all new human output is inside the `else` branch of `if json`
  - The `styled()` helper pattern is used across many command files (tag.rs, show.rs, register.rs, verify.rs, compose.rs)
---

## 2026-03-10 - P12-US-003
- Added treb.toml overwrite warning matching Go's migrate.go:126-138
- Non-interactive: plain text "Warning: treb.toml already exists and will be overwritten." to stderr
- Interactive: yellow "Warning: treb.toml already exists." to stderr + "Overwrite existing treb.toml?" confirmation prompt
- Updated existing `write_or_print_v2_cancelled_prompt_does_not_write` test → split into two tests:
  - `write_or_print_v2_cancelled_overwrite_prompt_does_not_write` (decline overwrite)
  - `write_or_print_v2_cancelled_write_prompt_does_not_write` (accept overwrite, decline write)
- Foundry-only warning (format_warning_banner with ⚠️) verified — Go has no equivalent path, format is Rust-specific
- Files changed: `crates/treb-cli/src/commands/migrate.rs`
- **Learnings for future iterations:**
  - The overwrite warning is inserted between dry_run early return and interactive preview in `write_or_print_v2_with_prompt`
  - Non-interactive condition is `yes || !stdin_is_tty`; json skips warnings entirely
  - Go non-interactive warning is plain text (no color), interactive is yellow via `color::WARNING`
  - Tests using `write_or_print_v2_with_prompt` with `treb_toml_existed: true` and `stdin_is_tty: true` must handle both prompts (overwrite then write)
---

## 2026-03-10 - P12-US-004
- Fixed 2 failing gen_deploy integration tests: `gen_deploy_human_success_block_is_printed_to_stdout` and `gen_deploy_library_guidance_matches_strategy`
- Root cause: tests compared stdout against plain text but `format_success` added ANSI color codes in subprocess (no TTY detection disables color, but `NO_COLOR` wasn't set)
- Fix: added `.env("NO_COLOR", "1")` and `--no-color` flag to test commands, matching the pattern used by other integration tests (cli_verify, cli_init, cli_list_show)
- All 538 unit tests pass, 15 gen_deploy + 11 prune/reset/migrate integration tests pass
- No references to old output text remain in test files
- Files changed: `crates/treb-cli/tests/cli_gen_deploy.rs`
- **Learnings for future iterations:**
  - Integration tests that assert on human-readable output text must use `.env("NO_COLOR", "1")` and `--no-color` to strip ANSI codes from subprocess output
  - The `color_enabled()` function defaults `COLOR_ENABLED` to `true`; without `NO_COLOR` env or `--no-color` flag, `format_success`/`format_error` will include ANSI escape sequences even when stdout is piped
  - Other tests using this pattern: cli_verify.rs, cli_init.rs, cli_list_show.rs
---

## 2026-03-10 - P12-US-005
- Verified all golden test expected files are already up-to-date (updated incrementally in prior review-fix commits)
- 11 gen-deploy commands.golden files have new "Generated deployment script" text + instruction lines
- 4 migrate commands.golden files have new success/warning/next-steps format
- 5 gen-deploy error/JSON golden files correctly unchanged (not affected by human output changes)
- 7 migrate JSON/registry/already_v2/no_treb_toml/dry_run golden files correctly unchanged
- Full `cargo test -p treb-cli` passes (zero failures), `cargo clippy --workspace --all-targets` clean
- No files changed (golden files were committed in P12-US-001 through P12-US-004 review fixes)
- **Learnings for future iterations:**
  - Golden files may already be updated by review-fix commits on prior stories — check test status before regenerating
  - Error case and JSON golden files are typically unaffected by human output format changes
  - `UPDATE_GOLDEN=1 cargo test -p treb-cli` can regenerate all golden files at once if needed
---
