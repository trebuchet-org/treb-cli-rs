{
  "project": "treb-cli-rs",
  "branchName": "feat/M3-P02-go-registry-coexistence-verification",
  "description": "Verify that the Rust CLI's registry stores are fully compatible with the Go CLI's registry format, enabling both CLIs to operate on the same .treb/ directory without data corruption. Fix the write path to output bare JSON maps instead of the wrapped format that breaks Go's json.Unmarshal.",
  "userStories": [
    {
      "id": "P2-US-001",
      "title": "Create Go Registry Test Fixtures from Production Data",
      "description": "As a developer, I want representative Go-created registry fixtures in the Rust test suite so that I can verify cross-CLI compatibility against real production data.",
      "acceptanceCriteria": [
        "crates/treb-registry/tests/fixtures/go-compat/deployments.json exists with 10-15 representative Go-created deployment entries",
        "crates/treb-registry/tests/fixtures/go-compat/transactions.json exists with 10-15 representative Go-created transaction entries",
        "crates/treb-registry/tests/fixtures/go-compat/safe-txs.json exists with 5-10 representative Go-created safe transaction entries",
        "All fixtures are bare JSON maps (no _format wrapper), matching Go output format",
        "Fixtures cover: SINGLETON, PROXY, LIBRARY types; CREATE, CREATE2, CREATE3 methods; null vs populated tags; null vs populated proxyInfo; present vs absent safeContext; present vs absent executedAt; timezone-offset timestamps",
        "cargo check -p treb-registry passes"
      ],
      "priority": 1,
      "passes": true,
      "notes": ""
    },
    {
      "id": "P2-US-002",
      "title": "Verify and Fix Rust Deserialization of Go-Created Store Files",
      "description": "As a developer, I want unit tests that load each Go-compat fixture file into the corresponding Rust store and verify all entries deserialize without errors, so that I can catch and fix any field mismatches.",
      "acceptanceCriteria": [
        "Test go_compat_deployments_deserialize loads all entries from Go fixture without errors",
        "Test go_compat_transactions_deserialize loads all entries from Go fixture without errors",
        "Test go_compat_safe_txs_deserialize loads all entries from Go fixture without errors",
        "Tests verify entry count matches expected fixture size",
        "Tests spot-check at least 2 entries per file for correct field values (address, chainId, status, etc.)",
        "Timezone-offset timestamps (e.g. 2026-03-09T18:05:36.422290725+01:00) parse correctly",
        "cargo test -p treb-registry passes",
        "cargo clippy -p treb-registry passes"
      ],
      "priority": 2,
      "passes": true,
      "notes": ""
    },
    {
      "id": "P2-US-003",
      "title": "Fix Rust Write Path for Go-Compatible Output",
      "description": "As a developer, I want write_versioned_file() to output bare JSON maps instead of the _format/entries wrapper so that files written by the Rust CLI remain readable by the Go CLI.",
      "acceptanceCriteria": [
        "write_versioned_file() writes bare JSON maps (sorted BTreeMap, 2-space indent, trailing newline) with no _format or entries wrapper",
        "read_versioned_file() still reads both wrapped format (backward compat) and bare format",
        "File locking behavior unchanged — write_versioned_file() still acquires exclusive lock",
        "Store round-trip test: write via write_versioned_file(), read back via read_versioned_file(), data matches",
        "Backward compat test: files with {\"_format\":\"treb-v1\",\"entries\":{...}} written by earlier Rust versions still load correctly",
        "cargo test -p treb-registry passes",
        "cargo clippy -p treb-registry passes"
      ],
      "priority": 3,
      "passes": false,
      "notes": ""
    },
    {
      "id": "P2-US-004",
      "title": "Verify Round-Trip Compatibility and Document Field Gaps",
      "description": "As a developer, I want to verify the full round-trip (load Go fixtures, modify via Rust, save, verify Go-parseable output) and document any field gaps between Go and Rust representations.",
      "acceptanceCriteria": [
        "Test go_compat_deployments_round_trip: load Go fixture, modify, save, verify bare JSON output with all expected keys and no extra keys",
        "Test go_compat_transactions_round_trip: same pattern",
        "Test go_compat_safe_txs_round_trip: same pattern",
        "Output JSON keys match Go JSON tags exactly (verified by comparing key sets)",
        "No Rust type uses #[serde(deny_unknown_fields)]",
        "Any compatibility gaps are documented in code comments on the relevant type definitions",
        "cargo test -p treb-registry passes",
        "cargo clippy -p treb-registry passes"
      ],
      "priority": 4,
      "passes": false,
      "notes": ""
    },
    {
      "id": "P2-US-005",
      "title": "CLI Integration Tests with Go Registry Data",
      "description": "As a developer, I want CLI integration tests that verify list, show, and tag commands work correctly against Go-created registry data end-to-end.",
      "acceptanceCriteria": [
        "Integration test: list against Go registry data shows correct deployment count and names",
        "Integration test: show <id> against Go registry data displays correct deployment details",
        "Integration test: tag --add core <id> modifies a Go-created deployment and the resulting deployments.json is still a bare JSON map (Go-compatible)",
        "Lookup index (.treb/lookup.json) is correctly built from Go-created deployments",
        "All integration tests pass with cargo test -p treb-cli",
        "cargo clippy --workspace --all-targets passes"
      ],
      "priority": 5,
      "passes": false,
      "notes": ""
    }
  ]
}
