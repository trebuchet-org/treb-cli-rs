# Ralph Progress Log
## Codebase Patterns
- Nested map registry stores should sort both outer and inner maps into `BTreeMap`s before `write_versioned_file()` so bare JSON output stays deterministic.

Started: Wed Mar 11 12:14:13 AM CET 2026
---
## 2026-03-11 00:22:16 CET - P9-US-001
- Implemented `AddressbookStore` with nested chain-id to name-to-address persistence, bare/wrapped JSON loading, deterministic writes, CRUD helpers, and empty-chain cleanup.
- Wired the store into `treb-registry` exports and the `Registry` facade with load/accessor/set/remove/list methods, plus init/open coverage to ensure `.treb/addressbook.json` is only created on first write.
- Files changed: `CLAUDE.md`, `crates/treb-registry/src/lib.rs`, `crates/treb-registry/src/registry.rs`, `crates/treb-registry/src/store/addressbook.rs`, `crates/treb-registry/src/store/mod.rs`, `.ralph/current/prd.json`, `.ralph/current/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered: `treb-registry` store files are written as bare JSON but must still load legacy wrapped `_format`/`entries` payloads through `read_versioned_file()`.
  - Gotchas encountered: `Registry::init()` should only create `.treb/` and must not eagerly materialize optional store files like `addressbook.json`.
  - Useful context: `Registry::open()` is the central place to instantiate and load new stores, and facade helpers belong directly on `Registry` alongside the other store delegates.
---
