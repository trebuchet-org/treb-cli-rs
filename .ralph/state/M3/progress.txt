## Codebase Patterns
- For CLI flag changes in `crates/treb-cli`, keep the derive parser in `src/main.rs`, the shell completion builder in `build.rs`, and a focused `parse_cli_from(...)` regression test in sync.

# Ralph Progress Log
Started: Tue Mar 10 09:09:42 PM CET 2026
---
## 2026-03-10 21:19:07 CET - P6-US-001
- Added long-only `--namespace`, `--network`, and `--no-fork` clap fields to `treb show` and kept runtime behavior unchanged.
- Mirrored the new `show` flags in shell completion generation and added parser tests for successful long-flag parsing plus rejection of `-n`/`-s`.
- Files changed: `.ralph/current/prd.json`, `.ralph/current/progress.txt`, `crates/treb-cli/src/main.rs`, `crates/treb-cli/build.rs`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    The CLI parser for `treb-cli` is split between clap derive definitions in `src/main.rs` and manual completion builders in `build.rs`; both must move together.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    Negative `parse_cli_from(...)` tests should use `match` rather than `unwrap_err()` unless the parsed `Cli` type implements `Debug`.
  - Useful context (e.g., "the evaluation panel is in component X")
    `Commands::Show` can safely accept parser-only fields before runtime wiring by matching with `..` in the main command dispatch.
---
