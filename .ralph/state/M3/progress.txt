# Ralph Progress Log
## Codebase Patterns
- When a clap flag becomes global in `crates/treb-cli/src/main.rs`, mirror it in `crates/treb-cli/build.rs` and refresh the affected `tests/golden/help_*` snapshots because the option propagates into root and subcommand help.

Started: Tue Mar 10 05:21:40 PM CET 2026
---
## 2026-03-10 19:57:38 CET - P5-US-001
- Implemented a global `--non-interactive` flag on `Cli`, removed duplicate `non_interactive` fields from `Run` and `Compose`, and routed the global value into those command handlers.
- Files changed: `crates/treb-cli/src/main.rs`, `crates/treb-cli/build.rs`, `crates/treb-cli/tests/golden/help_root/commands.golden`, `crates/treb-cli/tests/golden/help_gen/commands.golden`, `crates/treb-cli/tests/golden/help_completion/commands.golden`, `crates/treb-cli/tests/golden/help_config/commands.golden`, `CLAUDE.md`, `.ralph/current/prd.json`, `.ralph/current/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
  - Global clap flags must be defined in both `src/main.rs` and `build.rs` to keep runtime parsing and generated completions aligned.
  - The `integration_help` golden suite captures root help and representative subcommand help, so promoting a flag to global changes multiple `help_*` snapshots even when command behavior is otherwise unchanged.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
  - `cargo test -p treb-cli` includes extensive golden and e2e coverage; even parser-only CLI changes should expect snapshot churn before the full package test run goes green.
  - Useful context (e.g., "the evaluation panel is in component X")
  - Parser acceptance for root/global flags is covered cleanly in `crates/treb-cli/src/main.rs` unit tests via `parse_cli_from(...)`, which exercises the same normalized clap entry point used by `main()`.
---
