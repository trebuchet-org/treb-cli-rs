# Ralph Progress Log
## Codebase Patterns
- When a clap command supports both positional and flag forms for the same input, mirror the conflicts/required-group wiring in `crates/treb-cli/src/**` and `crates/treb-cli/build.rs`, and set `value_name` on renamed flag fields so usage/errors show the user-facing placeholder instead of the internal field id.
- When a dual-form fork subcommand also supports `--all`, put the positional arg, `--network`, and `--all` in the same ArgGroup in both clap definitions, then normalize them in the command dispatcher so the bypass path and conflict errors stay consistent.
- Golden tests that can early-return before exercising their fixture will not rewrite `commands.golden` under `UPDATE_GOLDEN=1`; if only the recorded argv changed, refresh that snapshot from a runnable environment or patch the header manually so the stored command matches the test case.
- For fork argument parsing coverage that only needs binary-level success/error assertions, add focused `TestContext::run(...)` checks in `crates/treb-cli/tests/integration_fork.rs` and reuse its loopback `spawn_json_rpc_server()` helper instead of creating new golden snapshots or spinning up full Anvil instances.

Started: Tue Mar 10 10:14:21 PM CET 2026
---
## 2026-03-10 22:25:54 CET - P7-US-001
- Implemented `fork enter` positional `<NETWORK>` support while keeping `--network` working, and added `--url` as a visible alias for `--rpc-url`.
- Added clap coverage for positional parsing, alias parsing, missing/conflicting network forms, and an integration test proving positional `fork enter <network> --url <rpc>` persists fork state correctly.
- Files changed: `CLAUDE.md`, `crates/treb-cli/build.rs`, `crates/treb-cli/src/commands/fork.rs`, `crates/treb-cli/tests/fork_integration.rs`, `.ralph/current/prd.json`, `.ralph/current/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y"): fork clap surfaces are defined twice, in the derive parser and in `crates/treb-cli/build.rs`, so positional/alias changes must be mirrored in both places.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W"): when the clap field name differs from the displayed long flag, missing `value_name` leaks the internal field id into usage and conflict errors.
  - Useful context (e.g., "the evaluation panel is in component X"): `crates/treb-cli/src/commands/fork.rs` already has local parser tests for subcommand shapes, while `crates/treb-cli/tests/fork_integration.rs` is the lightest place to verify persisted fork state against a live RPC.
---
## 2026-03-10 22:39:44 CET - P7-US-002
- Implemented positional `<NETWORK>` parsing for `fork exit`, `fork revert`, `fork restart`, `fork history`, and `fork diff` while preserving `--network`, and wired `fork exit --all` / `fork revert --all` through the same exclusivity rules.
- Added resolver helpers plus parser coverage for positional, flag, `--all`, and conflicting network forms, and mirrored the updated clap surface in shell-completion generation.
- Files changed: `CLAUDE.md`, `crates/treb-cli/build.rs`, `crates/treb-cli/src/commands/fork.rs`, `.ralph/current/prd.json`, `.ralph/current/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y"): fork subcommands that accept both positional and `--network` forms are normalized in `commands::fork::run(...)`, which is the right place to keep handler signatures stable while clap evolves.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W"): if a dual-form subcommand also has `--all`, the positional arg, long flag, and `--all` must share one ArgGroup in both parser definitions or the derive parser and generated completions drift.
  - Useful context (e.g., "the evaluation panel is in component X"): the fast coverage for fork argument-shape work lives in the `commands::fork::tests` parser block, so story-level validation does not need to wait on the heavier golden suites unless help text changes.
---
## 2026-03-10 22:50:34 CET - P7-US-003
- Added a dedicated `help_fork_enter` golden test and snapshot covering positional `<NETWORK>`, legacy `--network`, and the `--rpc-url` / `--url` alias surface.
- Refreshed fork-enter and fork-revert golden command headers to use the current canonical argv, and updated stale `--all` integration cases that were still combining `--all` with `--network`.
- Files changed: `CLAUDE.md`, `crates/treb-cli/tests/integration_help.rs`, `crates/treb-cli/tests/integration_fork.rs`, `crates/treb-cli/tests/golden/help_fork_enter/commands.golden`, `crates/treb-cli/tests/golden/fork_enter_already_forked/commands.golden`, `crates/treb-cli/tests/golden/fork_enter_json/commands.golden`, `crates/treb-cli/tests/golden/fork_enter_no_rpc_url/commands.golden`, `crates/treb-cli/tests/golden/fork_enter_not_initialized/commands.golden`, `crates/treb-cli/tests/golden/fork_enter_success/commands.golden`, `crates/treb-cli/tests/golden/fork_revert_json_no_active/commands.golden`, `crates/treb-cli/tests/golden/fork_revert_no_active_forks/commands.golden`, `.ralph/current/prd.json`, `.ralph/current/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y"): command-level help goldens belong in `crates/treb-cli/tests/integration_help.rs`, while fork behavior/output goldens stay in `crates/treb-cli/tests/integration_fork.rs`; keeping those split makes help-surface refreshes cheap.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W"): some golden tests can early-return before running their fixture, so `UPDATE_GOLDEN=1` will not rewrite the stored argv header in those cases even if the test case changed.
  - Useful context (e.g., "the evaluation panel is in component X"): the fork integration harness snapshots the executed argv in `commands.golden`, so canonical syntax changes often require touching both the test invocation and the matching golden header even when stdout/stderr stays identical.
---
## 2026-03-10 22:57:53 CET - P7-US-004
- Added focused subprocess integration tests for fork positional-vs-flag network filtering, missing-network failures, conflicting network forms, and `--url` alias persistence against `--rpc-url`.
- Recorded the reusable fork argument-testing pattern in `CLAUDE.md` so future iterations can use direct `TestContext` assertions plus the loopback JSON-RPC fixture instead of broad golden churn.
- Files changed: `CLAUDE.md`, `crates/treb-cli/tests/integration_fork.rs`, `.ralph/current/prd.json`, `.ralph/current/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y"): `crates/treb-cli/tests/integration_fork.rs` is the right place for fork subprocess assertions that care about parser/runtime behavior more than formatted output snapshots.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W"): missing/conflicting fork network arguments are rejected by clap before command dispatch, so subprocess tests should assert on clap stderr text rather than the later resolver helpers.
  - Useful context (e.g., "the evaluation panel is in component X"): the local `spawn_json_rpc_server()` fixture is enough for `fork enter` argument tests because the command only needs `eth_chainId`; no full Anvil process is required.
---
