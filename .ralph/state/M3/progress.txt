# Ralph Progress Log
Started: Tue Mar 10 02:02:08 PM CET 2026
## Codebase Patterns
- Foundry RPC endpoint parsing should reuse `treb_config::expand_env_vars()` after `foundry_config::Config::load_with_root()` because Foundry preserves `${VAR}` placeholders in endpoint strings.
---
## 2026-03-10 14:08 CET - P3-US-001
- Implemented `${VAR}` expansion for `foundry.toml` RPC endpoints by reusing `expand_env_vars()` inside `treb_config::rpc_endpoints()`.
- Added unit coverage for full env-var URLs, literal URLs, mixed literal/env-var URLs, and unset vars expanding to empty strings.
- Files changed: `crates/treb-config/src/foundry.rs`, `CLAUDE.md`, `.ralph/current/prd.json`, `.ralph/current/progress.txt`
- **Learnings for future iterations:**
  - `foundry_config::Config::load_with_root()` preserves raw `${VAR}` text for RPC endpoints, so treb must resolve those placeholders after loading the config.
  - Reusing `expand_env_vars()` keeps `${VAR}` behavior aligned between `treb.toml` account parsing and `foundry.toml` RPC endpoint parsing.
  - `cargo test -p treb-config` passes for this story.
---
