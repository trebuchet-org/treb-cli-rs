## 2026-03-03 - P20-US-009
- What was implemented: Final End-to-End Integration Test Suite
  - Created `crates/treb-cli/tests/e2e_workflow.rs` with 6 full multi-command workflow tests
  - `setup_project` helper: copies `gen-deploy-project` fixture (forge-std + SimpleContract.sol), writes `TrebDeploySimple.s.sol` deploy script that emits both `ContractDeployed` AND `TransactionSimulated` events, writes `treb.toml` with Anvil account 0 key, runs `treb init`
  - `run_deployment` helper: calls `treb run script/TrebDeploySimple.s.sol --rpc-url <url> --broadcast --non-interactive`
  - Tests: `e2e_init_run_list`, `e2e_run_show`, `e2e_run_tag_list_with_filter`, `e2e_run_prune_dry_run_clean`, `e2e_run_reset_list`, `e2e_list_no_color_has_no_ansi`
  - All 6 tests pass; `cargo test --workspace` passes
- Files changed: crates/treb-cli/tests/e2e_workflow.rs (new)
- **Learnings for future iterations:**
  - The pipeline's `hydrate_deployment` uses `format!("{:#x}", extracted.transaction_id)` — even `bytes32(0)` produces a non-empty string (`"0x000...0"`), so prune flags it as a broken ref
  - To get a "clean" registry (no broken refs), the deploy script must emit BOTH `ContractDeployed` (with a real non-zero txId) AND `TransactionSimulated` (with the same txId) so both records exist
  - Derive the txId in Solidity as `keccak256(abi.encode(block.chainid, block.number, address(deployed)))` — deterministic and unique per deployment
  - Solidity struct field types and ordering must exactly match the alloy `sol!` macro definitions in `treb-forge/src/events/abi.rs` for correct ABI event signature matching
  - `treb list --json` returns field `address` (not `contract_address`); `treb show --json` returns `contractName` and `chainId` (camelCase)
  - `treb reset --yes` skips the confirmation prompt; `treb prune --dry-run` is safe to run on a clean registry (outputs "Nothing to prune")
  - Reusing `gen-deploy-project` fixture avoids duplicating forge-std (large library) in a new fixture
---

## 2026-03-03 - P20-US-008
- What was implemented: Parasitic CI for Foundry Version Tracking
  - Created `.foundry-pin` at repo root containing the current pinned foundry tag (`v1.5.1`)
  - Created `.github/workflows/foundry-track.yml` with:
    - `schedule: cron: '0 6 * * *'` and `workflow_dispatch` triggers
    - `permissions: contents: write, pull-requests: write, issues: write`
    - Reads `.foundry-pin` and fetches latest foundry release tag via `gh release view --repo foundry-rs/foundry`
    - Skips if already up to date (pinned == latest)
    - Creates `foundry-bump/<tag>` branch: uses `sed` to replace old tag in Cargo.toml and updates `.foundry-pin`, then pushes
    - Runs `cargo check --workspace` and `cargo test --workspace` with `continue-on-error: true`; captures output with `tee` + `set -o pipefail`
    - On failure: `gh issue create` with truncated build logs; duplicate detection via `gh issue list --search "$TITLE in:title" --state open`
    - On success: `gh pr create` then `gh pr merge --auto --squash`
- Files changed: .foundry-pin (new), .github/workflows/foundry-track.yml (new)
- **Learnings for future iterations:**
  - `gh release view --repo owner/repo --json tagName -q .tagName` fetches the latest release tag without needing a checkout of that repo
  - `set -o pipefail` in bash steps is required when piping cargo output through `tee`; without it, `tee` exit code masks cargo failures and `continue-on-error` outcome reads `success` even on build failure
  - `gh issue list --search "title text in:title" --state open --json number -q '.[0].number'` is the correct syntax for duplicate issue detection
  - Store the tag (not commit SHA) in `.foundry-pin` for practical comparison against `gh release` output; tag-to-tag comparison is simpler and unambiguous
  - `head -c 8000 /tmp/log 2>/dev/null || echo "(no output)"` safely truncates captured logs for GitHub issue bodies (65 KB limit)
---

## 2026-03-03 - P20-US-007
- What was implemented: Release Packaging and trebup Installer Script
  - Created `.github/workflows/release.yml` triggered via `workflow_run` on "Release Build" completion for tags starting with `v`
  - Workflow downloads all 4 artifacts (`.tar.gz` + `.sha256` per target) using `actions/download-artifact@v4` with `merge-multiple: true`
  - Uses `gh release create` with `GH_REPO` env var (no checkout needed) to publish the GitHub Release
  - Created `scripts/trebup` as a POSIX sh installer script with executable bit set
  - `trebup` detects platform via `uname -s` / `uname -m`; maps to 4 targets (x86_64-linux-musl, aarch64-linux-musl, x86_64-darwin, aarch64-darwin)
  - Unsupported platforms exit with `"Unsupported platform: OS/ARCH"` message
  - Downloads archive and `.sha256` file to a temp dir; verifies with `sha256sum` (Linux) or `shasum -a 256` (macOS) before installing
  - Checksum failure exits non-zero without writing the binary to disk
  - `TREB_INSTALL_DIR` env var overrides install path (default: `~/.local/bin`)
  - `TREB_VERSION` env var overrides version to install (default: fetches latest from GitHub API)
  - `TREB_REPO` env var overrides the GitHub repository
- Files changed: .github/workflows/release.yml (new), scripts/trebup (new, executable)
- **Learnings for future iterations:**
  - `workflow_run` triggers have `github.event.workflow_run.head_branch` = tag name when triggered by tag push; use `startsWith(..., 'v')` to filter version tags
  - `actions/download-artifact@v4` with `run-id` + `github-token` + `merge-multiple: true` downloads all artifacts from a different workflow run into one directory
  - `gh release create` with `GH_REPO` env var doesn't need a checkout — eliminates a step in release workflows
  - POSIX sh `install -m 755 src dst` is the idiomatic way to copy + set permissions atomically
  - `trap 'rm -rf "$TMP_DIR"' EXIT INT TERM` cleans up temp dir on any exit
---

## 2026-03-03 - P20-US-006
- What was implemented: Cross-Compilation CI Matrix
  - Created `.github/workflows/release-build.yml` with a 4-target build matrix:
    - x86_64-unknown-linux-musl (ubuntu-latest + cross)
    - aarch64-unknown-linux-musl (ubuntu-latest + cross)
    - x86_64-apple-darwin (macos-latest + rustup target add)
    - aarch64-apple-darwin (macos-latest + rustup target add)
  - Linux targets install `cross` via `cargo install cross --locked` and build with `cross build --release`
  - macOS targets use `rustup target add <target>` and `cargo build --release`
  - Each target packages binary into `treb-<tag>-<target>.tar.gz` with sha256 checksum
  - SHA256 computed portably: `sha256sum` on Linux, `shasum -a 256` on macOS (command -v check)
  - Both `.tar.gz` and `.sha256` uploaded as named workflow artifacts via `actions/upload-artifact@v4`
  - Workflow triggers only on `v*.*.*` tags (`on.push.tags: ['v*.*.*']`)
  - Updated `ci.yml` fmt job to include `Swatinem/rust-cache@v2` (was previously missing)
- Files changed: .github/workflows/release-build.yml (new), .github/workflows/ci.yml (updated)
- **Learnings for future iterations:**
  - GitHub Actions `if: matrix.use_cross` works for boolean matrix fields; use `if: matrix.use_cross == false` for the inverse
  - `shell: bash` on matrix steps is needed for macOS runners (default shell is zsh on macOS GitHub Actions)
  - `echo "VAR=val" >> "$GITHUB_ENV"` passes env vars between steps; available in subsequent steps as `${{ env.VAR }}`
  - `cross` must be installed with `cargo install cross --locked`; `Swatinem/rust-cache@v2` with `key: ${{ matrix.target }}` ensures separate caches per target
  - sha256 command is not portable: use `command -v sha256sum` check to fall back to `shasum -a 256` on macOS
---

## 2026-03-03 - P20-US-005
- What was implemented: Shell completions and long-form help text
  - Added `clap_complete = "4"` to workspace `Cargo.toml` and `treb-cli/Cargo.toml` (both regular dep and build-dep)
  - Extended `crates/treb-cli/build.rs` with clap builder-API `build_cli()` function that mirrors the full CLI; generates bash/zsh/fish/elvish completions into `$OUT_DIR/completions/` at build time
  - Added `Completions { shell: String }` variant to `Commands` enum in `main.rs`; handler calls `Cli::command()` via `CommandFactory` + `clap_complete::generate()` to stdout at runtime
  - Added multi-line `///` doc comments (long_about) to all commands: Run, List, Show, Init, Config, Verify, Tag, Register, Sync, GenDeploy, Compose, Prune, Reset, Migrate, Fork, Completions
  - Added long-form `///` doc comments to `ForkSubcommand` variants (Enter, Exit, Revert, Restart, Status, History, Diff) in `fork.rs`
  - Added `#[command(long_about = "...")]` to `PruneArgs` and `ResetArgs` structs
  - Added long-form `///` doc comments to `MigrateSubcommand` variants (Config, Registry) in `migrate.rs`
  - Created `crates/treb-cli/tests/cli_completions.rs` with 7 integration tests covering bash/zsh/fish/elvish completions and `treb run --help` content
- Files changed: Cargo.toml, Cargo.lock, crates/treb-cli/Cargo.toml, crates/treb-cli/build.rs, crates/treb-cli/src/main.rs, crates/treb-cli/src/commands/fork.rs, crates/treb-cli/src/commands/migrate.rs, crates/treb-cli/src/commands/prune.rs, crates/treb-cli/src/commands/reset.rs, crates/treb-cli/tests/cli_completions.rs
- **Learnings for future iterations:**
  - `build.rs` cannot import from the binary crate being built; use clap's builder API (not derive) to define the CLI structure independently in build.rs
  - `clap_complete::generate_to(shell, &mut cmd, "treb", outdir)` writes to `outdir/treb.<ext>`; create the completions subdirectory first with `std::fs::create_dir_all`
  - `clap_complete::Shell::from_str("bash")` is the idiomatic way to parse a shell name at runtime; returns `Err(String)` (not anyhow) so wrap with `.map_err(...)`
  - `use clap::CommandFactory;` is needed to call `Cli::command()` from a derive-based Cli struct
  - For clap derive `Commands` variants, multi-line `///` doc comments make the first line the `about` and remaining lines the `long_about`
  - `#[command(long_about = "...")]` on `#[derive(Args)]` structs works for Prune/Reset even though they're used as `Commands::Prune(PruneArgs)` tuple variants
---

## 2026-03-03 - P20-US-004
- What was implemented: Colored output audit and NO_COLOR support
  - Added `owo-colors = { version = "4", features = ["supports-colors"] }` to workspace `Cargo.toml` and `treb-cli/Cargo.toml`
  - Created `crates/treb-cli/src/ui/color.rs` with `should_use_color()`, `color_enabled()`, and `const` palette constants (`STAGE`, `SUCCESS`, `WARNING`, `ERROR`, `MUTED`) of type `owo_colors::Style`
  - Added `--no-color` global flag to `Cli` struct in `main.rs`; calls `ui::color::color_enabled(cli.no_color)` early in `main()` which calls `owo_colors::set_override(false)` when disabled
  - Registered `pub mod color;` in `ui/mod.rs`
  - Added unit tests for `should_use_color()` with `NO_COLOR` env var and `TERM=dumb`
  - Compile-time `Send + Sync` check via const closure in `color.rs`
- Files changed: Cargo.toml, Cargo.lock, crates/treb-cli/Cargo.toml, crates/treb-cli/src/ui/mod.rs, crates/treb-cli/src/ui/color.rs, crates/treb-cli/src/main.rs
- **Learnings for future iterations:**
  - owo-colors v4 API: use `owo_colors::set_override(false)` to disable colors globally — NOT `owo_colors::control::set_should_colorize(false)` (that `control` module doesn't exist in v4)
  - `owo_colors::Style::new().cyan().bold()` etc. are all `const fn` in v4, so palette constants can be `const` values
  - `std::env::set_var` / `remove_var` are `unsafe` in Rust edition 2024 — require `unsafe {}` blocks in tests
  - Tests that manipulate env vars should save and restore the old value (especially `TERM`) to avoid polluting other tests
---

## 2026-03-04 - P1-US-002
- Renamed `ForkState.active_forks` field to `forks` so JSON serializes with top-level key `"forks"` (matching Go schema) instead of `"activeForks"`
- Updated all internal field accesses in `ForkStateStore` methods (get/insert/update/remove/list)
- Added `fork_state_top_level_keys_match_go_schema` test asserting `"forks"` and `"history"` keys present and `"activeForks"` absent
- Files changed:
  - crates/treb-core/src/types/fork.rs (field rename + new test)
  - crates/treb-registry/src/store/fork_state.rs (field accesses updated)
- **Learnings for future iterations:**
  - `#[serde(rename_all = "camelCase")]` on ForkState means `forks` serializes as `"forks"` (already camelCase), while `active_forks` was serializing as `"activeForks"`
  - ForkStateStore method names (list_active_forks, etc.) are the public API — they don't need renaming when only the internal struct field changes
  - All fork CLI commands use the store methods, not the struct field directly, so they needed no changes
---

## 2026-03-04 - P1-US-001
- Renamed SAFE_TXS_FILE from "safe_txs.json" to "safe-txs.json"
- Renamed GOVERNOR_PROPOSALS_FILE from "governor_proposals.json" to "governor-txs.json"
- Renamed FORK_STATE_FILE from "fork-state.json" to "fork.json"
- Updated all doc comments/strings referencing old filenames across 7 files
- Files changed:
  - crates/treb-registry/src/lib.rs (constant values)
  - crates/treb-registry/src/store/governor_proposals.rs (doc comments)
  - crates/treb-registry/src/store/safe_transactions.rs (doc comments)
  - crates/treb-registry/src/store/fork_state.rs (doc comments)
  - crates/treb-core/src/types/fork.rs (doc comment)
  - crates/treb-cli/src/commands/fork.rs (doc comments)
  - crates/treb-cli/tests/fork_integration.rs (doc comments + assertion messages)
- **Learnings for future iterations:**
  - All code uses constants (not string literals), so changing the constant value propagates everywhere
  - Only doc comments/strings needed manual updating — grep for old filename patterns to find them all
  - cargo test --workspace --lib runs all 421 unit tests across all crates
---

## Codebase Patterns
- File constants defined in `crates/treb-registry/src/lib.rs` — all stores reference these constants, changing values there propagates automatically
- `registry.list_deployments()` returns `Vec<&Deployment>` (borrows from registry); clone with `.into_iter().cloned().collect()` to get owned `Vec<Deployment>` for passing to selector functions that take `&[Deployment]`.
- `build.rs` cannot import from the binary crate being built — use clap's builder API (not derive macros) to define the CLI structure independently for completion generation.
- `clap_complete::Shell::from_str("bash")` parses shell name; `generate(shell, &mut Cli::command(), "treb", &mut io::stdout())` for runtime output; `generate_to` for file output.
- owo-colors v4: disable colors via `owo_colors::set_override(false)` (not `owo_colors::control::set_should_colorize` — that path doesn't exist). `Style::new().cyan().bold()` etc. are `const fn`.
- `std::env::set_var`/`remove_var` require `unsafe {}` in Rust edition 2024; always restore env vars in tests (save old value, restore after test).
- When changing a clap positional argument from `String` to `Option<String>`, existing tests that expect the old "argument required" error must be updated to match the new fuzzy-selector error messages.
- `prune.rs` and `reset.rs` use struct-based args (`PruneArgs`, `ResetArgs`) passed to `run(args)` — no separate `run` signature; import `crate::ui::prompt::confirm` via full path since the crate is `treb-cli` (binary, not library in module scope).
- `treb-cli` is a binary crate; `lib.rs` was created for the `ui` module to be testable. `main.rs` keeps `mod commands; mod output; mod ui;`.
- `TrebError::Cli(String)` variant added to `treb-core/src/error.rs` for CLI/interaction errors.
- `dialoguer` requires `features = ["fuzzy-select"]` for `FuzzySelect`; declared in workspace Cargo.toml.
- `fuzzy_select_deployment` uses a lifetime `'a` to tie the returned ref to the input slice.
- Tests for non-TTY behavior skip when `console::Term::stdout().is_term()` to avoid hanging in interactive sessions.
- `DeploymentStrategy` fields: `method`, `salt`, `init_code_hash`, `factory`, `constructor_args`, `entropy` (no `kind`/`factory_address`).
- `ArtifactInfo` fields: `path`, `compiler_version`, `bytecode_hash`, `script_path`, `git_commit`.
- `VerificationInfo` fields: `status`, `etherscan_url`, `verified_at`, `reason`, `verifiers: HashMap`.

# Ralph Progress Log
Started: Tue Mar  3 10:18:41 PM CET 2026
---

## 2026-03-03 - P20-US-003
- What was implemented: Interactive network selector and batch multiselect
  - `run.rs`: added `use console::Term` and `use crate::ui::selector::fuzzy_select_network`; when `--network` is `None`, `!non_interactive`, and TTY detected, loads foundry RPC endpoints and presents `fuzzy_select_network` for interactive selection
  - `verify.rs`: added `use console::Term` and `use crate::ui::selector::multiselect_deployments`; in `run_batch`, candidates are first collected as owned `Vec<Deployment>`, then TTY-checked: TTY presents `multiselect_deployments` for subset selection, non-TTY verifies all
  - Both `fuzzy_select_network` and `multiselect_deployments` were already implemented in `ui/selector.rs` from earlier work; this story just wired them into the commands
- Files changed: crates/treb-cli/src/commands/run.rs, crates/treb-cli/src/commands/verify.rs
- **Learnings for future iterations:**
  - Network names for fuzzy selector come from `treb_config::rpc_endpoints(foundry_cfg).keys()` — already available since `ensure_initialized` guarantees foundry.toml exists
  - When restructuring to use multiselect in batch, collect owned `Vec<Deployment>` first (with `.cloned()`), then pass `&[Deployment]` to selector — avoids registry borrow conflicts
  - An early-return when `candidate_ids.is_empty()` after multiselect is needed in case user selects nothing
---

## 2026-03-03 - P20-US-002
- What was implemented: Wired deployment fuzzy selector into commands
  - Added `fuzzy_select_deployment_id` convenience wrapper to `ui/selector.rs`
  - `show.rs` and `tag.rs`: deployment arg changed from `&str` to `Option<String>`; fuzzy selector called when `None`
  - `verify.rs`: removed early bail on missing deployment; fuzzy selector now used when `deployment` is `None` and `!all`
  - `prune.rs` and `reset.rs`: replaced manual `io::stdin().read_line` confirmation with `crate::ui::prompt::confirm()`; removed unused `io::{self, Write}` imports
  - Updated `main.rs`: `Show.deployment` and `Tag.deployment` changed from `String` to `Option<String>`
  - Fixed pre-existing test compilation error in selector.rs (temporary value borrow issue)
  - Updated `cli_verify.rs` test to expect new error messages ("no deployment selected" / "no TTY")
- Files changed: ui/selector.rs, commands/show.rs, commands/tag.rs, commands/verify.rs, commands/prune.rs, commands/reset.rs, main.rs, tests/cli_verify.rs
- **Learnings for future iterations:**
  - `registry.list_deployments()` returns `Vec<&Deployment>`; must clone with `.into_iter().cloned().collect::<Vec<_>>()` before passing to selectors expecting `&[Deployment]`
  - `cargo check` passes even with temporary-value borrow errors that only surface during test compilation (`cargo test`); always run `cargo test` not just `cargo check`
  - Changing positional clap args from required to optional changes UX: old "argument required" error messages become selector/TTY errors

## 2026-03-03 - P20-US-001
- What was implemented: Fuzzy deployment selector infrastructure
  - Added `nucleo-matcher`, `dialoguer` (with `fuzzy-select` feature), `console` to workspace Cargo.toml and treb-cli Cargo.toml
  - Added `TrebError::Cli(String)` variant to `treb-core/src/error.rs`
  - Created `crates/treb-cli/src/lib.rs` exposing `pub mod ui`
  - Created `crates/treb-cli/src/ui/mod.rs`, `ui/selector.rs`, `ui/prompt.rs`
  - Added `mod ui;` to `main.rs` (binary also has direct access)
- Files changed: Cargo.toml, Cargo.lock, crates/treb-cli/Cargo.toml, crates/treb-core/src/error.rs, crates/treb-cli/src/lib.rs, crates/treb-cli/src/main.rs, crates/treb-cli/src/ui/{mod,selector,prompt}.rs
- **Learnings for future iterations:**
  - `dialoguer` v0.11 needs `features = ["fuzzy-select"]` for `FuzzySelect` — not enabled by default
  - `fuzzy_select_deployment` returns borrowed `&Deployment` so needs explicit `'a` lifetime
  - Unit tests for non-TTY behavior must guard with `if is_term() { return; }` to skip in interactive terminals
  - The actual struct field names for `DeploymentStrategy` are `method/salt/init_code_hash/factory/constructor_args/entropy`, not `kind/factory_address/salt`
---
