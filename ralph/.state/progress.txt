# Ralph Progress Log
Started: Wed Mar  4 05:27:51 AM CET 2026

## Codebase Patterns
- TestWorkdir auto-creates `.treb/` directory — cannot test "no .treb/" path via golden file framework; use a fixture without `foundry.toml` instead for "uninitialized project" error tests
- EpochNormalizer in `framework/normalizer.rs` replaces epoch timestamps in backup paths (e.g., `prune-<EPOCH>`, `reset-<EPOCH>`)
- The `regex` crate does NOT support look-behind; use capture groups with alternation instead (e.g., `(prune|reset)-(\d{10,13})`)
- For prune/reset/migrate golden tests, seed registry data via `post_setup_hook` using `Registry::open()` + `insert_deployment()`/`insert_transaction()` directly
- `compose-project` fixture is empty (just `.gitkeep`) — use for tests that need no foundry.toml
---

## 2026-03-04 - P9-US-001
- Implemented 7 golden-file integration tests for `treb prune`: prune_dry_run, prune_destructive, prune_nothing, prune_chain_filter, prune_include_pending, prune_json_dry_run, prune_uninitialized
- Added `EpochNormalizer` to `framework/normalizer.rs` for backup path normalization
- Files changed:
  - `crates/treb-cli/tests/integration_prune.rs` (new)
  - `crates/treb-cli/tests/framework/normalizer.rs` (added EpochNormalizer)
  - `crates/treb-cli/tests/golden/prune_*/output.golden` (7 new golden files)
- **Learnings for future iterations:**
  - TestWorkdir always creates `.treb/` — uninitialized tests must use a fixture without `foundry.toml`
  - `regex` crate doesn't support look-behind — use capture groups with alternation for prefix-aware matching
  - Registry seeding for prune tests: create clean pair (dep-ok/tx-ok), broken pairs (dep→missing tx, tx→missing dep), and pending tx
  - `PruneCandidateKind` Display impl gives exact string used in table: BrokenTransactionRef, BrokenDeploymentRef, etc.
---
