diff --git a/crates/script/src/lib.rs b/crates/script/src/lib.rs
index 4ba843338..36ec81cc5 100644
--- a/crates/script/src/lib.rs
+++ b/crates/script/src/lib.rs
@@ -63,9 +63,9 @@
 use serde::Serialize;
 use std::path::PathBuf;
 
-mod broadcast;
-mod build;
-mod execute;
+pub mod broadcast;
+pub mod build;
+pub mod execute;
 mod multi_sequence;
 mod progress;
 mod providers;
@@ -246,7 +246,7 @@ pub struct ScriptArgs {
 
 impl ScriptArgs {
     /// Loads config, resolves evm_opts (including network inference from fork), and returns them.
-    async fn resolved_evm_opts(&self) -> Result<(Config, EvmOpts)> {
+    pub async fn resolved_evm_opts(&self) -> Result<(Config, EvmOpts)> {
         let (config, mut evm_opts) = self.load_config_and_evm_opts()?;
 
         // Auto-detect network from fork chain ID when not explicitly configured.
@@ -255,7 +255,7 @@ async fn resolved_evm_opts(&self) -> Result<(Config, EvmOpts)> {
         Ok((config, evm_opts))
     }
 
-    async fn preprocess<FEN: FoundryEvmNetwork>(
+    pub async fn preprocess<FEN: FoundryEvmNetwork>(
         self,
         config: Config,
         mut evm_opts: EvmOpts,
