// SPDX-License-Identifier: UNLICENSED
pragma solidity ^0.8.0;

import {Script, console} from "forge-std/Script.sol";
import { Counter } from "../src/Counter.sol";

contract DeployCounter is Script {
    function run() public {
        vm.startBroadcast();

        // TODO: Set constructor arguments
        uint256 initialCount = 0; // TODO: replace placeholder
        address _owner = address(0); // TODO: replace placeholder

        Counter deployed = new Counter(initialCount, _owner);

        console.log("Counter deployed at:", address(deployed));

        vm.stopBroadcast();
    }
}
